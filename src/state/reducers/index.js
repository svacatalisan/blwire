// default reducer
// Note: You can remove this reducer and create your own reducer

export default (state: any = {}, action: any) => {
    switch(action.type) {
        default:
            return state;
    }
}